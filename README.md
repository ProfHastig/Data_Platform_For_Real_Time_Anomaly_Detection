Data platform consisting of three main components:

- a service that serves an anomaly detection model (isolation forest)
- the monitoring platform Prometheus
- Grafana

The service runs inside multiple Docker components.  
